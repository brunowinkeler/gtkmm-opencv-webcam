#if !defined(CAMERA_H)
#define CAMERA_H

namespace camera_tools
{
    class CameraWidget
    {
    private:
        /* data */
    public:
        CameraWidget(/* args */);
        ~CameraWidget();
    };

} // namespace camera_tools

#endif // CAMERA_H